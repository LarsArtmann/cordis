# Historical point-in-time snapshots: kept verbatim, never re-linted.
docs/status/
docs/planning/

# Upstream-owned: everything under packages/ must stay byte-identical to
# upstream (non-TS files are CI-guarded by the upstream-parity check), so it
# is never re-linted or reflowed.
packages/

# Vendored installs and build artifacts.
node_modules/
target/
zig-out/
